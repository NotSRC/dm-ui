import { IntegerMaskDirective } from './integer-mask.directive';


describe('IntegerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IntegerMaskDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
