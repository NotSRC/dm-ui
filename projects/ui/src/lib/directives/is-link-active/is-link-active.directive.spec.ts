import { IsLinkActiveDirective } from './is-link-active.directive';

describe('IsLinkActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IsLinkActiveDirective();
    expect(directive).toBeTruthy();
  });
});
