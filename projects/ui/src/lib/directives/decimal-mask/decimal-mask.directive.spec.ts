import { DecimalMaskDirective } from './decimal-mask.directive';


describe('DecimalDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalMaskDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
